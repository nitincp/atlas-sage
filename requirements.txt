# --- LLM gateway (model-agnostic routing, local + cloud) ---
litellm

# --- Local model inference ---
ollama

# --- Embeddings ---
sentence-transformers          # BGE-M3 and others

# --- Vector / graph store ---
lancedb
pyarrow

# --- Community detection (Leiden/Louvain) ---
networkx
leidenalg
python-igraph

# --- HuggingFace model hub (download, model cards, snapshots) ---
huggingface_hub

# --- AST parsing (tree-sitter grammar packages added per skill) ---
tree-sitter>=0.23.0
tree-sitter-c-sharp

# --- Config ---
python-dotenv

# --- Dev / test toolchain ---
pytest
pytest-asyncio
ruff
